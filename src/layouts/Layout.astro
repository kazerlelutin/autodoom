---
import Header from '@/components/Header.astro'
export interface Props {
  title: string
}

const { title } = Astro.props as Props

/**
 * //TODO
 * Creer le form pour proposer une machine

Mise en place des mails de modération machines

recherche auto par machine --> save des articles

tri des articles par chatgpt

écriture des articles par chatgpt

génération des podcasts par chatgpt

bouton de report.

FOOTER --> about, tout le reste. 

Creer le form pour porposer un articles ? (ajouté dans la listes)
 */
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <script>
      import { webVitals } from '../lib/vitals'
      import '@/styles/globals.css'

      const analyticsId = import.meta.env.PUBLIC_VERCEL_ANALYTICS_ID

      if (analyticsId) {
        webVitals({
          path: location.pathname,
          params: location.search,
          analyticsId,
        })
      }
    </script>
  </head>
  <body class="bg-doom-bg grid grid-rows-[auto_1fr_auto] h-screen">
    <Header />
    <div class="relative bg-slate-800 h-full">
      <main class="absolute top-0 bottom-0 left-0 right-0 p-2">
        <slot />
      </main>
    </div>

    <footer>About - git - legal notice - ko-fi</footer>
  </body>
</html>
